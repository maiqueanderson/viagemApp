import React from 'react';
import { View, Text, Button } from 'react-native';

const CriarViagem = ({ navigation }) => {
  return (
   <View>
    <Text>Criar viagem works</Text>
   </View>
  );
};

export default CriarViagem;
